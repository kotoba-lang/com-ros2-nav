{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([ros2_nav.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'ros2_nav.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
